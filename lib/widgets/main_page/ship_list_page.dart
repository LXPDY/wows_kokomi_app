
import 'package:flutter/material.dart';

class ShipListPage extends StatelessWidget {
  const ShipListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('TODO:船只列表');
  }
}
